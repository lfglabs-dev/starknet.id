import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#19aa6e",
      light: "#dcf2e9",
    },
    secondary: {
      main: "#402d28",
      light: "#eae0d5",
    },
    background: {
      default: "#ffffff",
    },
  },
});

export default theme;
